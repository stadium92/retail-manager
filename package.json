{
  "name": "retail-manager-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "install:all": "npm install && cd frontend && npm install && cd ../backend/local-bridge && npm install",
    "clean:dist": "rm -rf frontend/dist",
    "build:backend": "./scripts/build-sidecar.sh",
    "tauri:build": "npm run clean:dist && npm run build:backend && npm run build --prefix frontend && tauri build && mkdir -p release && cp -R src-tauri/target/release/bundle/macos/*.app release/ && cp -R src-tauri/target/release/bundle/dmg/*.dmg release/"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  },
  "optionalDependencies": {
    "@tauri-apps/cli-win32-arm64-msvc": "2.10.0"
  },
  "dependencies": {
    "minimist": "^1.2.8",
    "puppeteer": "^24.36.1"
  }
}
