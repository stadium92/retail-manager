PORT=8787
DATA_DIR=./data
JWT_SECRET=change-me

# Supabase project this branch's backend syncs to (sync_outbox drain).
# SUPABASE_SERVICE_KEY must be the service_role key, not the anon key.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
