Okay, so this prompt below should not be implemented, but you can make PRDs, generate prompts, more input, and small empty files that explain what we should do, what we should not, and how we should proceed. I think about everything, all right, and at the end, giving it kind of a summary of everything.

I've already submitted the prompt to Claude 4.6, so at the end we will just compare the two prompts. The PRDs, some of them, up together, take what's good, what's bad, and kind of proceed with the implementation. First, you should just do the PRDs, the prompts, brainstorm everything; you should not do anything else. 

THE PROMPT:{
Hey chat. We have a new feature, I think. It's not a bug fix but a new feature. So we need to improve the navigation of the workers' side of the project. It should be designed for workers that are not kind of illiterate. They are kind of illiterate, and we need to make the app kind of easy to use so they can just use it very easily. So thought about making a nice subdirectory in the source folder of the frontend, maybe we can call it "Navigation". We make all the navigation code logics under that directory, and we just kind of take the imports the code from the there,but this is TypeScript. I don't know much about its structure. Maybe it behaves differently from Python because if I were using Python, I would just create a whole module/repo dedicated to it and just import it on other modules or from other subrepos and repos. And this actually makes the codes more maintainable, so we can just add more features and changes. So I will leave it to look in the internet and use your knowledge to list all the relevant libraries, already implemented libraries that I can use to improve the code based on those features I will mention in this prompt. So we actually need to think like a system engineer or a code architect in order to pull this off. I think maybe we have to even rewrite all the repos in order to implement this implementation, but I don't know, I know you are a system architect, you are very smart. We can think like a team of system architects that are trying to pull this off because it can be hard. 

Okay, so now the navigation parts should be easy to use, so I can just use the arrows first to navigate through the modules. Let's say I have the sales, purchases, files, edition, management, and all these modules. I can just use the navigation button to just navigate through all the modules straight. And for the navigation button, we are using the arrows on the keyboard. 

so when I click on the left arrow, it just moves to the next. I need this per default. Like the moment you open or log in the woker dashboard the page, it actually works, but I need that the moment you open the page, you can just navigate through it right away. 

Also, really important, under the sales sub-modules, on the sales table. When I enter the sales table, there should be a row already logged in for where the cursor is at per default inside the modules, on the tables where you log in the products. I can just type straight the name of the product under the collumns, not only the designation collumn where the cursor resides per default, and when i click enter there should be kind of a dropdown of the search menu. The dropdown of the search menu will just kind of search through straight the product and shows the relevant search based on the name typed, so when i click on enter the dropdown menu shows with the relevant products, and after clicking the Enter button and choosing the product, there should be a default hover box on the row of the designation column. When I click on the right arrow, it navigates to the SKU columns. I can click Enter to modify the SKU column (but per default it should be hovering) and type what I want. I click Enter again and navigate to the next column with the right arrow, which is packaging. When I click on Enter, the packaging toggles.I just click again on the right arrow, which is stock, is should first type enter to in order to be able to modify modify the stock and type enter to save and exit. I can just click again on the right arrow; it goes to price retail. I can modify the price from there, but each time i need to type enter to select and modify the box and then enter again to exit. When I toggle again, the quantity goes to discount, and when I toggle again, it goes to the total. 

Okay! I just found a conflict that may appear when we are hovering. We can just click, and everything is kind of reset. This is a problem, so I need you to really deeply think on that, on these kinds of situation and find a solution and just decide at the end of the prompt. You let me know what you have chosen, what is the best approach to this, because this needs deep thinking. This part of the prompting thinking. Don't just go through it; just read through, analyze it, and decide what we should do. Also, a conflict may happen with the scanner mode. So, when the front-end detects a scanner, right? When it detects a scanner, it just jumps to the next row, straight. That's what I need. So, when it detects a kind of a... very fast scanner on another designation column. It should just log in on the next row straight on the next row, ready to enter the next product. This should happen on so on like scan next row scan next row scan next row. So I don't know; this is a suggestion. Maybe there's a better way to do it, so please need you to actually really think deeply on this on When you're reaching the total column and I click on Enter, just log in the next  empty row. Where we can just start all over again and just type whatever product you want to sell again, or maybe you want to scan; you just scan it. And also please, the overlay should be per default on the first row under the designation column please. And please, we need to think of a button we can click and that will just bypass all the RO navigation and just log in the second row. We need to think on this button. It shouldn't be the enter button because the enter button selects the box on the row of the assigned column. It should kind of act like a toggle button. So it can be the enter button. So we should think of another button, but I don't know that button, you can just suggest me that this button. 

So actually, this navigation is kind of bounded to the table. It's bounded to inside the table. And please, I need this feature on all the sub-modules of the sales. The Retail Sales sub-module, the Retail Billing sub-module, the Wholesale Billing sub-module, the Performance Invoice sub-module. 

This is actually sales focus. This feature is meant to be really sales-focused and speed execution. Maybe we have a kind of rush hour and people are coming in. A lot of people are coming in the store, so should be able to sell really fast. So these features are meant to make the sales easier and faster. You can even improve on those suggestions because I know you are smart and you already have a team you are thinking about that will pull this off. 
}