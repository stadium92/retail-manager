the app should meet the requirement of the client thus we should add easy to use features so that any worker/store-teller/seller appart from the store owner who is educated and know his way around computer to use it.


to note: the store already have bar code scanners so we should use their already in place ressources, plus they already have an dedicated computer and screen for their work. the brand of the scanner is "OBZ", plus the client asked features like having the receipt for the, an easy to use interface (any uneducated store teller can just show the item to the bar code, maybe if necessary set the price then cashout the product for the client), i should also more into the already implemented app of the client plus it is to note that the computer they have is an "intel dual processor" plus and 2.5gb of ram, so we should make the app to support the requirement. they also need many MVP receipt and store inventory tracking features we should implement. and also i should the software to support the OBZ bar code scanner, plus i should learn about how their logiciel was already able to identify the bar codes, where they already saved or coded/programmed? or does the scanner already support that? their BAR code scanner model is "RF-70" so i wonder if there may me an library already on github with the open source software that i can use instead of actually rebuilding the whole current one.